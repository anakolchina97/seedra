import * as React from "react"
import { Link } from "gatsby"
import styled from "styled-components"

const Header = ({ siteTitle }) => <HeaderWrap>Header</HeaderWrap>

const HeaderWrap = styled.header``

export default Header
